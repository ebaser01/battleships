import React  from 'react';

const Header = (props) => {
    
    return(
        <div className='title-area'>
            <h1>
            BATTLESHIPS
            </h1>
        </div>
    );
};

export default Header;